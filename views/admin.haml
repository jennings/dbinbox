= haml(:_signin, :layout => false)

.row
  %h1.col-sm-12
    %img(class="modal-h-img sprite s_upload_32 modal-h-img" src="img/icon_spacer.gif")
    Change settings for #{@user.display_name}'s Dropzone

.row
  .col-sm-12
    %p
      If you are the owner of this Dropbox, you can change the
      access code needed to upload files.
    %p
      Submitting this form will also relink Dropzone to your Dropbox account if it
      has been accidentally unlinked from your Dropbox settings.
    %p
      When saving settings, you will be sent to Dropbox to
      verify you own the account. Click "Allow" to allow this
      change.

%form.form-horizontal{action: "/admin", id: 'admin_form', method: 'post'}
  .form-group
    %label.col-sm-2.control-label(for="access_code") New Access Code
    .col-sm-4
      %input.form-control{:type => "text", :name => "access_code", :id => "access_code"}
  .form-group
    %label.col-sm-2.control-label(for="clear_access_code") Clear Access Code
    .col-sm-4
      %input.form-control(type="checkbox" name="clear_access_code" id="clear_access_code")
  .form-group
    .col-sm-offset-2.sm-col-4
      %input.btn.btn-primary(type="submit" value="Set code")

.row
  %h1.col-sm-12
    %img(class="modal-h-img sprite s_application_delete_32 modal-h-img" src="img/icon_spacer.gif")
    Delete #{@user.display_name}'s Dropzone account

.row
  .col-sm-12
    %p
      Pressing the button below will unlink Dropzone from your Dropbox account.
    %p
      The URL <code>#{url(@user.username)}</code> will be available for anyone to register
      with a different Dropbox account.

%form.form-horizontal{action: "/delete", id: 'unlink_form', method: 'post'}
  .form-group
    %label.col-sm-2.control-label(for="delete_confirmation") Type "DELETE"
    .col-sm-4
      %input.form-control(type="text" name="delete_confirmation" id="delete_confirmation")
  .form-group
    .col-sm-offset-2.sm-col-4
      %input#delete_button.btn.btn-danger.disabled(type="submit" value="Delete account" disabled="disabled")

.clearfix
