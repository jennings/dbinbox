.col-sm-4.col-sm-offset-8.text-right
  - if session[:registered]
    = session[:username]
    %a{href: url("/admin")} settings
    |
    %a{href: url("/logout")} sign out
  - else
    %a{href: url("/login")} sign in
